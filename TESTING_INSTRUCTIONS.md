# 🧪 Тестирование приложения

## ✅ Supabase настроен!

Теперь можно протестировать приложение.

---

## 📋 Шаг 1: Тестирование локально

### 1. Запустите приложение

```bash
cd /Users/mansurusupov/tg-finance-miniapp/web
npm run dev
```

Должно появиться:
```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### 2. Откройте в Telegram Mini App

1. Откройте Telegram
2. Найдите вашего бота: `@lmyfinancel_bot` (или как вы назвали)
3. Отправьте `/start`
4. Нажмите кнопку "Открыть приложение"

### 3. Проверьте работу

**Что проверить:**
- ✅ Приложение открывается
- ✅ Данные загружаются (если есть)
- ✅ Можно добавить счет/транзакцию
- ✅ Данные сохраняются

**Проверка консоли:**
1. В Telegram Mini App откройте консоль разработчика:
   - Нажмите на экран 3 раза быстро
   - Или используйте веб-версию Telegram и F12
2. Проверьте, нет ли ошибок:
   - ❌ "API not configured" — проверьте `.env` файл
   - ❌ "NOT_SUBSCRIBED" — убедитесь, что подписаны на канал `@mansurmlrd`
   - ❌ "Failed to load from server" — проверьте Edge Function в Supabase

---

## 📋 Шаг 2: Проверка синхронизации

### 1. Добавьте данные

1. Добавьте счет или транзакцию
2. Подождите 2-3 секунды (фоновая синхронизация)

### 2. Проверьте в Supabase

1. Откройте Supabase → **"Table Editor"**
2. Проверьте таблицу `user_data`:
   - Должны появиться записи с вашим `telegram_user_id`
   - В поле `key` должны быть: `accounts`, `transactions`, и т.д.
   - В поле `value` должны быть ваши данные (JSON)

### 3. Проверьте таблицу `users`

1. В таблице `users` должна быть запись с вашим `telegram_user_id`
2. Поле `is_subscriber` должно быть `true`
3. Поле `subscriber_checked_at` должно содержать время проверки

---

## 📋 Шаг 3: Проверка офлайн-режима

### 1. Отключите интернет

1. В настройках устройства отключите Wi-Fi/мобильные данные
2. Или используйте режим "Самолет"

### 2. Проверьте работу

- ✅ Приложение должно открываться
- ✅ Данные должны загружаться из localStorage
- ✅ Можно добавлять/редактировать данные
- ✅ При включении интернета данные синхронизируются

---

## ❓ Проблемы?

### Ошибка "API not configured"

**Причина:** Файл `.env` не найден или переменные не загружены.

**Решение:**
1. Проверьте, что файл `web/.env` существует
2. Проверьте, что переменные начинаются с `VITE_`
3. Перезапустите `npm run dev`

### Ошибка "NOT_SUBSCRIBED"

**Причина:** Вы не подписаны на канал `@mansurmlrd`.

**Решение:**
1. Откройте канал: https://t.me/mansurmlrd
2. Подпишитесь на канал
3. Обновите приложение в Telegram

### Ошибка "Failed to load from server"

**Причина:** Edge Function не работает или секреты не настроены.

**Решение:**
1. Проверьте Edge Function в Supabase → Edge Functions → `sync`
2. Проверьте логи: Edge Functions → `sync` → Logs
3. Убедитесь, что секреты добавлены: Settings → Edge Functions → Secrets

### Данные не сохраняются

**Причина:** Ошибка в Edge Function или проблемы с сетью.

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Network tab
3. Проверьте логи Edge Function в Supabase

---

## ✅ Успешное тестирование

Если все работает:
- ✅ Данные загружаются с сервера
- ✅ Данные сохраняются на сервер
- ✅ Приложение работает офлайн
- ✅ Синхронизация происходит в фоне

---

## 📝 Что дальше?

После успешного тестирования:
1. Добавьте GitHub Secrets (для деплоя)
2. Задеплойте на GitHub Pages
3. Протестируйте на продакшене
