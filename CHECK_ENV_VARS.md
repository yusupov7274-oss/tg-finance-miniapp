# 🔍 Как проверить переменные окружения

## ❌ Проблема: `import.meta` не работает в консоли

Ошибка `SyntaxError: import.meta is only valid inside modules` означает, что `import.meta.env` нельзя использовать напрямую в консоли браузера.

---

## ✅ Решение: Проверьте через код приложения

### Способ 1: Добавить временный код в приложение

Я добавлю временный код, который выведет переменные окружения в консоль при загрузке.

### Способ 2: Проверить через Network tab

1. F12 → **Network** (Сеть)
2. Попробуйте добавить данные в приложении
3. Найдите запрос к `sync`
4. Нажмите на запрос
5. Проверьте **Request Headers**:
   - Должен быть `Authorization: Bearer ...`
   - Должен быть `apikey: ...`

---

## ✅ Решение: Откройте приложение в Telegram Mini App

**Важно:** Приложение должно быть открыто **в Telegram**, а не просто в браузере!

### Как открыть в Telegram:

1. Откройте Telegram (на телефоне или в приложении)
2. Найдите вашего бота
3. Отправьте `/start`
4. Нажмите кнопку "📊 Открыть приложение"
5. **Тогда** откройте консоль (F12 в веб-версии Telegram)

---

## 🧪 Альтернативный способ проверки

Вместо `import.meta.env` используйте проверку через Network tab:

1. F12 → **Network** (Сеть)
2. Попробуйте добавить данные в приложении
3. Найдите запрос к `sync`
4. Проверьте:
   - **Request Headers** → должны быть `Authorization` и `apikey`
   - **Status** → должен быть 200 (не 401)

---

## 📝 Что проверить:

1. ✅ **Открыто ли приложение в Telegram Mini App?** (не просто в браузере)
2. ✅ **Есть ли запросы к `sync` в Network tab?**
3. ✅ **Есть ли `Authorization` header в запросе?**
4. ✅ **Какой статус ответа?** (200 или 401)
