# 🔧 Как открыть консоль браузера в Telegram Mini App

## 📋 Способ 1: Через веб-версию Telegram (рекомендуется)

### 1. Откройте веб-версию Telegram

1. Откройте браузер (Chrome, Safari, Firefox)
2. Перейдите на: https://web.telegram.org
3. Войдите в свой аккаунт Telegram

### 2. Откройте вашего бота

1. Найдите вашего бота в списке чатов
2. Отправьте `/start`
3. Нажмите кнопку "📊 Открыть приложение"

### 3. Откройте консоль разработчика

1. Нажмите **F12** (или **Cmd+Option+I** на Mac, или **Ctrl+Shift+I** на Windows/Linux)
2. Откроется панель разработчика
3. Перейдите на вкладку **"Console"** (вверху панели)

### 4. Выполните код

1. Внизу консоли будет поле для ввода
2. Скопируйте и вставьте код
3. Нажмите **Enter**

---

## 📋 Способ 2: Через мобильное приложение (сложнее)

### Для iOS:

1. Подключите iPhone к Mac через USB
2. На Mac: Откройте Safari → Develop → [Ваш iPhone] → [Telegram]
3. Откроется консоль разработчика

### Для Android:

1. Включите "Отладка по USB" в настройках разработчика
2. Подключите к компьютеру
3. Откройте Chrome → chrome://inspect → Devices
4. Найдите Telegram и откройте консоль

**Но проще использовать веб-версию Telegram!**

---

## 📋 Способ 3: Через прямой URL (если приложение открывается в браузере)

### 1. Откройте приложение напрямую

1. Откройте браузер
2. Перейдите на: https://yusupov7274-oss.github.io/tg-finance-miniapp/
3. Нажмите **F12** (или **Cmd+Option+I** на Mac)
4. Перейдите на вкладку **"Console"**

---

## 🧪 Код для проверки переменных окружения

Откройте консоль и выполните:

```javascript
console.log('URL:', import.meta.env.VITE_SUPABASE_URL);
console.log('Key:', import.meta.env.VITE_SUPABASE_ANON_KEY);
```

**Ожидаемый результат:**
```
URL: https://zhchkxukgltknfbropqu.supabase.co
Key: sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS
```

**Если пусто:**
- GitHub Secrets не добавлены
- Или не задеплоено после добавления секретов

---

## 🧪 Код для теста запроса

Откройте консоль и выполните:

```javascript
const initData = window.Telegram?.WebApp?.initData;
const url = 'https://zhchkxukgltknfbropqu.supabase.co/functions/v1/sync';
const key = 'sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS';

console.log('InitData:', initData ? 'Есть ✅' : 'Нет ❌');

if (initData) {
  fetch(url, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${key}`,
      'apikey': key,
      'x-telegram-init-data': initData
    },
    body: JSON.stringify({ accounts: [] })
  })
  .then(r => {
    console.log('Status:', r.status);
    return r.json();
  })
  .then(data => {
    console.log('Response:', data);
    if (data.success) {
      console.log('✅ Успешно!');
    } else {
      console.error('❌ Ошибка:', data.error);
    }
  })
  .catch(err => console.error('❌ Ошибка:', err));
}
```

---

## 📝 Пошаговая инструкция:

1. ✅ Откройте https://web.telegram.org
2. ✅ Войдите в Telegram
3. ✅ Найдите вашего бота
4. ✅ Откройте приложение (кнопка "📊 Открыть приложение")
5. ✅ Нажмите **F12** (или **Cmd+Option+I** на Mac)
6. ✅ Перейдите на вкладку **"Console"**
7. ✅ Вставьте код и нажмите **Enter**

---

## ❓ Проблемы?

**Не открывается консоль:**
- Попробуйте правой кнопкой мыши → "Inspect" или "Проверить элемент"
- Или используйте меню браузера: View → Developer → Developer Tools

**Не видно вкладку Console:**
- В панели разработчика найдите вкладки вверху
- Нажмите на "Console"

**Код не выполняется:**
- Убедитесь, что вы в поле ввода внизу консоли
- Нажмите Enter после вставки кода
