# 🚀 Как запустить бота

## ⚠️ Важно:

Бот должен быть **постоянно запущен**, чтобы отвечать на команды в Telegram.

---

## 📋 Способ 1: Запуск в терминале (для тестирования)

### 1. Откройте терминал

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
node index.js
```

### 2. Должно появиться:

```
✅ Бот запущен
📱 Web App URL: https://yusupov7274-oss.github.io/tg-finance-miniapp/
Bot is running...
```

### 3. Проверьте работу

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должна появиться кнопка "📊 Открыть приложение"

**⚠️ Не закрывайте терминал!** Если закрыть, бот перестанет работать.

---

## 📋 Способ 2: Запуск в фоне (рекомендуется)

### 1. Запустите бота в фоне

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
nohup node index.js > bot.log 2>&1 &
```

### 2. Проверьте, что бот запущен

```bash
ps aux | grep "node index.js" | grep -v grep
```

Должна появиться строка с процессом.

### 3. Остановить бота (если нужно)

```bash
pkill -f "node index.js"
```

---

## 📋 Способ 3: Использование PM2 (для продакшена)

### 1. Установите PM2

```bash
npm install -g pm2
```

### 2. Запустите бота через PM2

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
pm2 start index.js --name "telegram-bot"
```

### 3. Сохраните конфигурацию

```bash
pm2 save
pm2 startup
```

### 4. Полезные команды PM2

```bash
pm2 list          # Список процессов
pm2 logs           # Логи
pm2 restart all    # Перезапустить
pm2 stop all       # Остановить
pm2 delete all     # Удалить
```

---

## ❓ Проблемы?

### Ошибка "BOT_TOKEN не установлен"

**Решение:**
1. Проверьте файл `.env` в папке `bot/`
2. Убедитесь, что токен правильный
3. Перезапустите бота

### Ошибка "getaddrinfo ENOTFOUND api.telegram.org"

**Причина:** Нет интернета или проблемы с сетью.

**Решение:**
1. Проверьте интернет-соединение
2. Попробуйте использовать VPN (если нужно)
3. Перезапустите бота

### Бот не отвечает

**Проверка:**
1. Убедитесь, что бот запущен
2. Проверьте логи: `cat bot.log` (если использовали nohup)
3. Проверьте, что токен правильный
4. Попробуйте перезапустить бота

---

## ✅ Проверка работы

После запуска бота:

1. Откройте Telegram
2. Найдите вашего бота (по токену или username)
3. Отправьте `/start`
4. Должна появиться кнопка "📊 Открыть приложение"

---

## 🔄 Автозапуск при перезагрузке

Если используете PM2:

```bash
pm2 startup
pm2 save
```

Бот будет автоматически запускаться при перезагрузке компьютера.
