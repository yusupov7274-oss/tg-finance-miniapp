# Следующие шаги после создания проекта Supabase

## ✅ Проект создан! Теперь:

### 1️⃣ Получите ключи (2 минуты)

1. В левом меню нажмите **⚙️ Settings** (шестеренка)
2. Выберите **"API"**
3. Скопируйте и **сохраните**:
   - **Project URL**: `https://xxxxx.supabase.co` ← скопируйте
   - **anon public key**: `eyJhbGc...` ← скопируйте
   - **service_role key**: `eyJhbGc...` ← скопируйте (секретный!)

**💾 Сохраните эти ключи — они понадобятся для Шага 3!**

---

### 2️⃣ Создайте таблицы (3 минуты)

1. В левом меню выберите **"SQL Editor"** (иконка с `</>`)
2. Нажмите **"New query"**
3. Откройте файл `supabase_migration.sql` в вашем проекте
4. **Скопируйте ВЕСЬ код** из файла (Ctrl+A, Ctrl+C)
5. Вставьте в SQL Editor (Ctrl+V)
6. Нажмите **"Run"** (или `Ctrl+Enter` / `Cmd+Enter`)
7. ✅ Должно появиться: **"Success. No rows returned"**

**Проверка:** В левом меню выберите **"Table Editor"** — должны появиться таблицы `users` и `user_data`.

---

### 3️⃣ Создайте Edge Function (5 минут)

1. В левом меню выберите **"Edge Functions"** (иконка с молнией ⚡)
2. Нажмите **"Create a new function"**
3. Название функции: `sync` (маленькими буквами)
4. Нажмите **"Create function"**
5. Откройте файл `supabase_edge_function_sync.ts` в вашем проекте
6. **Скопируйте ВЕСЬ код** из файла (Ctrl+A, Ctrl+C)
7. Вставьте в редактор Edge Function (Ctrl+V)
8. Нажмите **"Deploy"** (кнопка справа вверху)
9. ✅ Должно появиться: **"Function deployed successfully"**

---

### 4️⃣ Добавьте секреты (2 минуты)

1. В **Project Settings** → **"Edge Functions"** → **"Secrets"**
2. Нажмите **"Add new secret"**

**Секрет 1:**
- **Name**: `BOT_TOKEN`
- **Value**: `7568002456:AAFpZOYNjGXbYxaeZ-fdQwliyCApbpZ7B3A`
- Нажмите **"Add secret"**

**Секрет 2:**
- **Name**: `TELEGRAM_CHANNEL_ID`
- **Value**: `-1001781839176`
- Нажмите **"Add secret"**

✅ Готово! Секреты добавлены.

---

## ✅ Проверка

После выполнения всех шагов:

- ✅ Таблицы созданы (проверьте в "Table Editor")
- ✅ Edge Function `sync` создана (проверьте в "Edge Functions")
- ✅ Секреты добавлены (проверьте в Settings → Edge Functions → Secrets)

---

## 📝 Что дальше?

После завершения всех 4 шагов, **сообщите мне**, и мы перейдем к **Шагу 3: Создание API клиента** для связи приложения с Supabase.

**Важно:** Сохраните Project URL и anon key — они понадобятся для следующего шага!

---

## ❓ Проблемы?

**Не вижу SQL Editor:**
- Убедитесь, что проект полностью загрузился (может занять 1-2 минуты)
- Обновите страницу (F5)

**Ошибка в SQL:**
- Убедитесь, что скопировали весь код из `supabase_migration.sql`
- Проверьте, нет ли уже таблиц с такими именами

**Edge Function не деплоится:**
- Убедитесь, что скопировали весь код из `supabase_edge_function_sync.ts`
- Проверьте синтаксис (не должно быть ошибок в редакторе)

**Не могу добавить секреты:**
- Убедитесь, что Edge Function `sync` создана
- Попробуйте обновить страницу
