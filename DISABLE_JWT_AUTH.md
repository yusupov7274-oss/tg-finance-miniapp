# 🔧 Отключение JWT авторизации в Edge Function

## ❌ Проблема: 401 Unauthorized

Supabase Edge Functions по умолчанию требуют JWT авторизацию, но мы используем кастомную авторизацию через Telegram initData.

---

## ✅ Решение: Отключить JWT проверку

### Вариант 1: Через конфигурацию (если используете CLI)

Создайте файл `supabase/functions/sync/.supabase/functions.json`:

```json
{
  "auth": false
}
```

Но это работает только при деплое через CLI.

---

## ✅ Решение: Исправить создание клиента

Я обновил код Edge Function — теперь он использует упрощенное создание клиента без дополнительных headers.

### 1. Обновите Edge Function

1. Supabase → Edge Functions → `sync` → **Code**
2. Найдите создание клиента (около строки 117)
3. Замените на:

```typescript
// Получаем переменные окружения Supabase
// Если не доступны, используем значения напрямую
const supabaseUrl = Deno.env.get("SUPABASE_URL") || "https://zhchkxukgltknfbropqu.supabase.co";
const supabaseAnonKey = Deno.env.get("SUPABASE_ANON_KEY") || "sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS";

// Создаем клиент Supabase
// Используем anon key для создания клиента (не требует JWT)
const supabaseClient = createClient(supabaseUrl, supabaseAnonKey);
```

### 2. Задеплойте

1. Нажмите **"Deploy"**
2. Дождитесь завершения

---

## 🔍 Альтернативное решение: Использовать service_role key

Если проблема останется, можно использовать `service_role` key для создания клиента (он не требует JWT).

Но для этого нужно:
1. Добавить `SUPABASE_SERVICE_ROLE_KEY` как секрет в Edge Function
2. Использовать его вместо `anon` key

---

## 🧪 Проверка после обновления

1. Обновите Edge Function (см. выше)
2. Задеплойте
3. Проверьте Invocations — должны быть 200 OK
4. Проверьте таблицы — должны появиться данные

---

## ❓ Если проблема останется

Проверьте:
1. ✅ Правильно ли обновлен код?
2. ✅ Задеплоена ли функция?
3. ✅ Есть ли ошибки в логах?

Можно также попробовать использовать `service_role` key вместо `anon` key.
