# 🚀 Быстрое решение: Бот не отвечает

## ❌ Проблема:

Бот не отвечает на команду `/start` в Telegram.

---

## 🔍 Причина:

Бот не может подключиться к `api.telegram.org`:
- Нет интернета
- VPN блокирует доступ
- Telegram заблокирован в вашей стране

---

## ✅ Решение 1: Проверьте подключение

### 1. Отключите VPN (если включен)

### 2. Проверьте интернет

Откройте браузер и проверьте, работает ли интернет.

### 3. Запустите бота заново

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
node index.js
```

**Должно появиться:**
```
✅ Бот запущен
📱 Web App URL: https://yusupov7274-oss.github.io/tg-finance-miniapp/
Bot is running...
```

**Если видите ошибки `getaddrinfo ENOTFOUND`** — переходите к Решению 2.

---

## ✅ Решение 2: Используйте прокси (если Telegram заблокирован)

### 1. Установите пакет для прокси

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
npm install socks-proxy-agent
```

### 2. Обновите `bot/index.js`

Замените начало файла на:

```javascript
import "dotenv/config";
import TelegramBot from "node-telegram-bot-api";
import { SocksProxyAgent } from 'socks-proxy-agent';

const webAppUrl = process.env.WEBAPP_URL;

// Настройка прокси (если указан в .env)
let botOptions = { polling: true };
if (process.env.PROXY_URL) {
  const agent = new SocksProxyAgent(process.env.PROXY_URL);
  botOptions.request = { agent };
  console.log("🔒 Используется прокси:", process.env.PROXY_URL);
}

const bot = new TelegramBot(process.env.BOT_TOKEN, botOptions);
```

### 3. Добавьте прокси в `.env`

Откройте `bot/.env` и добавьте (если у вас есть прокси):

```
PROXY_URL=socks5://127.0.0.1:1080
```

**Где взять прокси:**
- Используйте свой VPN с SOCKS5 прокси
- Или используйте публичный прокси (не рекомендуется для продакшена)

### 4. Перезапустите бота

```bash
node index.js
```

---

## ✅ Решение 3: Используйте другой способ запуска

### Вариант A: Через веб-версию Telegram

1. Откройте https://web.telegram.org
2. Найдите вашего бота
3. Отправьте `/start`

### Вариант B: Проверьте, что бот запущен на сервере

Если бот должен работать постоянно, используйте:
- **PM2** (см. `bot/START_BOT.md`)
- **nohup** (см. `bot/START_BOT.md`)
- **Docker** (для продакшена)

---

## 🧪 Проверка работы

### 1. Запустите бота

```bash
cd /Users/mansurusupov/tg-finance-miniapp/bot
node index.js
```

### 2. Проверьте в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. **Должна появиться кнопка "📊 Открыть приложение"**

---

## ❓ Если ничего не помогло

1. **Проверьте токен бота:**
   - Откройте https://api.telegram.org/bot7568002456:AAFpZOYNjGXbYxaeZ-fdQwliyCApbpZ7B3A/getMe
   - Должен вернуться JSON с информацией о боте

2. **Проверьте логи:**
   - Посмотрите, какие ошибки показывает бот
   - Пришлите скриншот ошибок

3. **Проверьте, что бот запущен:**
   - Убедитесь, что процесс `node index.js` работает
   - Не закрывайте терминал, где запущен бот

---

## 💡 Важно:

**Бот должен быть постоянно запущен!** Если вы закрываете терминал, бот перестает работать.

Для постоянной работы используйте PM2 (см. `bot/START_BOT.md`).
