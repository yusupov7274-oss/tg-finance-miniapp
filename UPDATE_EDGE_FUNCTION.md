# 🔧 Обновление Edge Function для исправления 401 ошибок

## ❌ Проблема:

**401 Unauthorized** — запросы отклоняются до выполнения функции.

**Причина:** Edge Function требует правильную авторизацию Supabase.

---

## ✅ Решение: Обновите Edge Function

### 1. Откройте Edge Function в Supabase

1. Supabase → Edge Functions → `sync`
2. Перейдите на вкладку **"Code"**

### 2. Найдите строку с `createClient`

Найдите эту часть кода (около строки 117):

```typescript
const supabaseClient = createClient(
  Deno.env.get("SUPABASE_URL") ?? "",
  Deno.env.get("SUPABASE_ANON_KEY") ?? "",
  {
    global: {
      headers: { Authorization: req.headers.get("Authorization")! },
    },
  }
);
```

### 3. Замените на:

```typescript
// Получаем переменные окружения Supabase (автоматически доступны в Edge Functions)
const supabaseUrl = Deno.env.get("SUPABASE_URL") ?? "";
const supabaseAnonKey = Deno.env.get("SUPABASE_ANON_KEY") ?? "";

// Создаем клиент Supabase с правильной авторизацией
const supabaseClient = createClient(supabaseUrl, supabaseAnonKey, {
  global: {
    headers: {
      Authorization: req.headers.get("Authorization") || `Bearer ${supabaseAnonKey}`,
      apikey: supabaseAnonKey,
    },
  },
});
```

### 4. Сохраните и задеплойте

1. Нажмите **"Deploy"** (кнопка справа вверху)
2. Дождитесь завершения деплоя
3. Проверьте работу

---

## 🔍 Альтернативное решение: Проверьте переменные окружения

Supabase Edge Functions автоматически предоставляют переменные окружения:
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`

Но если они не доступны, нужно использовать значения напрямую или проверить настройки.

---

## 🧪 Проверка после обновления

1. Обновите Edge Function (см. выше)
2. Задеплойте
3. Откройте приложение в Telegram
4. Попробуйте добавить данные
5. Проверьте Invocations — должно быть меньше 401 ошибок

---

## 📝 Если проблема останется

Проверьте:
1. ✅ Переменные окружения доступны в Edge Function?
2. ✅ Правильный ли `SUPABASE_ANON_KEY`?
3. ✅ Правильный ли `SUPABASE_URL`?

Можно также использовать значения напрямую (не рекомендуется для продакшена):

```typescript
const supabaseUrl = "https://zhchkxukgltknfbropqu.supabase.co";
const supabaseAnonKey = "sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS";
```
