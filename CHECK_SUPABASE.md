# 🔍 Проверка Supabase: Данные не появляются

## 📋 Пошаговая проверка:

### 1. Проверьте ключи в Supabase Dashboard

1. Откройте: https://supabase.com/dashboard
2. Выберите ваш проект
3. Settings → **API**
4. Скопируйте:
   - **Project URL** (должен быть: `https://zhchkxukgltknfbropqu.supabase.co`)
   - **anon public key** (должен начинаться с `sb_publishable_...`)

**Сравните с вашими ключами:**
- URL: `https://zhchkxukgltknfbropqu.supabase.co`
- Key: `sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS`

**Если не совпадает:**
- Обновите `.env` файл
- Обновите GitHub Secrets
- Перезапустите деплой

---

### 2. Проверьте Edge Function

1. Supabase → **Edge Functions**
2. Должна быть функция `sync`
3. Статус должен быть **"Active"**

**Проверьте логи:**
1. Edge Functions → `sync` → **Logs**
2. Откройте приложение в Telegram
3. Попробуйте добавить данные
4. Проверьте логи — должны появиться записи

**Если нет логов:**
- Edge Function не вызывается
- Проверьте URL в коде
- Проверьте консоль браузера (F12)

---

### 3. Проверьте секреты в Edge Function

1. Supabase → Settings → **Edge Functions** → **Secrets**
2. Должны быть:
   - `BOT_TOKEN` = `7568002456:AAFpZOYNjGXbYxaeZ-fdQwliyCApbpZ7B3A`
   - `TELEGRAM_CHANNEL_ID` = `-1001781839176`

**Если секретов нет:**
- Добавьте их (см. инструкцию выше)

---

### 4. Проверьте подписку на канал

1. Откройте канал: https://t.me/mansurmlrd
2. Убедитесь, что вы **подписаны**
3. Откройте приложение в Telegram
4. Проверьте консоль (F12) — не должно быть ошибки "NOT_SUBSCRIBED"

---

### 5. Используйте тестовую страницу

Я создал тестовую страницу для проверки подключения:

1. Откройте файл: `web/test-supabase.html` в браузере
2. **Или** загрузите его на GitHub Pages и откройте через Telegram Mini App
3. Нажмите кнопки для проверки:
   - "Проверить" (переменные окружения)
   - "Проверить" (Telegram WebApp)
   - "Загрузить данные" (GET запрос)
   - "Сохранить тестовые данные" (POST запрос)

**Что проверить:**
- ✅ Все проверки должны пройти успешно
- ✅ GET запрос должен вернуть данные (или пустой объект)
- ✅ POST запрос должен вернуть `{ success: true }`

---

### 6. Проверьте консоль браузера

1. Откройте приложение в Telegram
2. Нажмите F12 (в веб-версии) или откройте консоль разработчика
3. Перейдите на вкладку **"Console"**
4. Попробуйте добавить данные
5. Проверьте ошибки

**Возможные ошибки:**

**A. "API not configured"**
- GitHub Secrets не добавлены
- Решение: добавьте секреты в GitHub

**B. "Failed to load from server"**
- Edge Function не работает
- Решение: проверьте Edge Function и логи

**C. "NOT_SUBSCRIBED"**
- Не подписаны на канал
- Решение: подпишитесь на `@mansurmlrd`

**D. CORS ошибки**
- Проблема с настройками Edge Function
- Решение: проверьте CORS headers

---

### 7. Проверьте таблицы в Supabase

1. Supabase → **Table Editor**
2. Проверьте таблицу `users`:
   - Должна быть запись с вашим `telegram_user_id`
   - `is_subscriber` должно быть `true`

3. Проверьте таблицу `user_data`:
   - Должны быть записи с вашими данными
   - `key` должен содержать: `accounts`, `transactions`, и т.д.

**Если таблицы пустые:**
- Данные не сохраняются
- Проверьте логи Edge Function
- Проверьте консоль браузера

---

## 🧪 Тест в консоли браузера

Откройте приложение в Telegram, нажмите F12 → Console и выполните:

```javascript
// Получить initData
const initData = window.Telegram.WebApp.initData;
console.log('InitData:', initData);

// Проверить подключение
fetch('https://zhchkxukgltknfbropqu.supabase.co/functions/v1/sync', {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer sb_publishable_m96BQEXNJw5_L_CI0kQkUg_0vdPodFS',
    'x-telegram-init-data': initData
  }
})
.then(r => r.json())
.then(console.log)
.catch(console.error);
```

**Ожидаемый результат:**
- Должен вернуться JSON с данными или ошибкой
- Если ошибка — проверьте текст ошибки

---

## ❓ Частые проблемы

### Проблема 1: "API not configured"

**Причина:** Переменные окружения не доступны на GitHub Pages

**Решение:**
1. Проверьте GitHub Secrets (Settings → Secrets and variables → Actions)
2. Добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
3. Перезапустите деплой

### Проблема 2: "NOT_SUBSCRIBED"

**Причина:** Не подписаны на канал

**Решение:**
1. Подпишитесь на `@mansurmlrd`
2. Обновите приложение

### Проблема 3: Данные не сохраняются

**Причина:** Edge Function не работает или ошибка в коде

**Решение:**
1. Проверьте логи Edge Function (Supabase → Edge Functions → sync → Logs)
2. Проверьте консоль браузера (F12)
3. Проверьте секреты в Edge Function

---

## 🆘 Если ничего не помогло

Пришлите:
1. Скриншот консоли браузера (F12 → Console)
2. Скриншот логов Edge Function (Supabase → Edge Functions → sync → Logs)
3. Скриншот таблиц в Supabase (Table Editor)
4. Результаты тестовой страницы (`test-supabase.html`)
